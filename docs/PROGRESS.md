# Journey Engine — Core Runtime Implementation Progress

This file is the single source of truth for implementation state. Any fresh
Claude Code session (or fresh Claude chat session) should read this file plus
the two design docs to orient before doing anything.

## Reference documents (in this repo)
- `docs/journey_engine_prd_v2.md` — product requirements (what & why)
- `docs/journey_engine_eng_design.md` — engineering design (how); §11 is the build order

## Target environment
- Godot 4.6.x stable, GDScript only, Compatibility renderer
- Core classes live in `journey_core/`
- Throwaway manual-test scaffolding lives in `tests/`

## Core invariants (must hold at every step — never violate)
- **Single mutation path:** game/test code never writes the Blackboard directly;
  all state change flows through consequences applied by the runtime. (eng §9)
- **Presentation-agnostic core:** no class in `journey_core/` instantiates a Node,
  touches the SceneTree, or assumes a UI exists. (eng §5.5)
- **Missing-key policy:** missing resource key reads as 0.0 + warning; missing flag
  reads as false (no warning); consequences never auto-create undeclared resources. (eng §4.3)
- **Studio-authorable:** every resource must be fully constructable/editable in the
  Godot inspector with no code. (eng §1.3)
- **Determinism:** fixed RNG seed + identical state ⇒ identical stochastic results. (eng §1.3)

## Build order & status
- [x] **Step 1** — Resource classes: Condition, Consequence, ConditionGroup, Choice,
      Event, ResourceDef, Config
- [x] **Step 2** — Blackboard + initialization
- [x] **Step 3** — Evaluator (conditions) + Mutator (consequences), as pure helpers + tests
- [x] **Step 4** — SequenceManager deterministic routing + `_enter_event` + signals;
      minimal test scene proving the loop end-to-end
- [x] **Step 5** — PoolIndex + stochastic pull + weighted pick + tests
- [x] **Step 6** — SaveManager + versioning + round-trip tests
- [ ] **Step 7** — `validate()` authoring utility
- [ ] **Step 8** — Export smoke test (Web/WASM) + small sample game exercising every feature

## Session log (append one line per completed step)
<!-- e.g. "2026-05-28 — Step 1 complete. 7 resource classes in journey_core/. Tested via inspector." -->
2026-05-28 — Step 1 complete. Created 7 resource classes in journey_core/ (JourneyCondition, JourneyConsequence, JourneyConditionGroup, JourneyChoice, JourneyEvent, JourneyResourceDef, JourneyConfig) matching eng design §3.2–§3.7. Pure data, no methods. Manual inspector verification passed: project loads error-free, all 7 types appear in New Resource dialog, enums render as dropdowns, test_condition.tres and test_event.tres saved.
2026-05-28 — Step 2 complete. Added journey_core/blackboard.gd as a RefCounted (class_name Blackboard) matching eng design §4.1 fields (resources, flags, metadata, rng) and §4.2 init (clamped defaults, copied initial_flags, deterministic-or-randomized rng with stored seed, turn_counter/current_event_id/history/seen_ids primed). No mutation/evaluation/signal logic — those land in Steps 3–4. NOTE comment flags that Step 4 may relocate the call site to JourneyRuntime, keeping the logic itself on Blackboard. Test scaffolding in tests/ (test_config.tres, test_blackboard.gd, test_blackboard.tscn) — manual run pending user verification.
2026-05-28 — Step 3 complete. Added journey_core/evaluator.gd (class_name JourneyEvaluator) and journey_core/mutator.gd (class_name JourneyMutator) as RefCounted with static methods only, per eng §2 / §8.2 (headless-testable, no SceneTree, no Autoload). Evaluator implements all 8 operators + ALL/ANY/null/empty group rules with the §4.3 missing-key policy exact (warn+0 for resource reads citing event id when available, silent-false for flags). Mutator implements ADD/SUBTRACT/SET_VALUE with §4.4 clamp to JourneyResourceDef bounds and skip+warn on undeclared keys; SET_FLAG/TOGGLE_FLAG untouched. apply_batch applies the full batch first, THEN scans config.resource_defs in definition order and RETURNS triggered defs (== min_value with bottom_out_event, or == max_value with top_out_event); routing is deferred to Step 4. No signals emitted, no metadata mutation. Manual test (tests/test_eval_mutate.gd, .tscn) run by user and all 30 checks PASS: numeric ops, missing-resource warn+0, silent-false flag, null/empty/ALL/ANY groups, ADD/SUBTRACT clamp on gold to 0, SET/TOGGLE flags, undeclared-mana consequence skipped with warn, batch reporting two defs in definition order [sanity, rations], single-trigger, and top-out symmetry.
2026-05-28 — Step 4 complete. Added journey_core/journey_runtime.gd (Node, autoload-registered as `JourneyRuntime`; NO class_name, since the autoload's auto-global collides with a class_name of the same name — "Class 'JourneyRuntime' hides an autoload singleton") as the single public surface per eng §9: blackboard ownership, the six §5.3 signals declared verbatim, get_resource/has_flag/get_metadata convenience reads, and stubbed save/load/validate/rebuild_pool. Routing brain lives in journey_core/sequence_manager.gd (class_name JourneySequenceManager, RefCounted) — start_new_journey relocates the Blackboard.initialize call site (logic stays on Blackboard, per Step-2 NOTE) and emits journey_started; process_choice snapshots touched resource/flag keys, calls JourneyMutator.apply_batch, then diffs post-clamp values to emit per-mutation resource_changed/flag_changed (keeping the Mutator pure); routing precedence is forced-bottom/top-out > target_event > continue_to_pool (currently emits a clean "Step 5" journey_error, no crash) > end-journey on the current event. _enter_event guards null, bumps turn_counter, updates current_event_id, appends to a 200-cap ring-buffer history, marks seen_ids, filters visible choices via JourneyEvaluator.eval_group (filtering lives in core per Dumb-UI contract §5.5), and emits event_changed. Autoload registered directly in project.godot ([autoload] JourneyRuntime="*res://journey_core/journey_runtime.gd"). Test scaffolding: tests/journey/{evt_start,evt_rich,evt_kind}.tres (the spec's 3-event journey, with evt_kind's "They join you" gated by ALL[HAS_FLAG helped_stranger]), tests/test_config.tres updated to point start_event at evt_start, tests/game_view.gd + .tscn as a dumb Label+VBox UI that connects all six signals and prints state changes. User verified manually: loop advances on clicks, gold gains 50 on "Take the gold" (resource_changed fires post-clamp), helped_stranger flag fires on "Help the stranger", "They join you" appears when the flag passes and disappears when its condition key is rewritten to an unset key (visibility filtering bites), and journey_ended fires on terminal choices.
2026-05-28 — Step 5 complete. Added journey_core/pool_index.gd (class_name JourneyPoolIndex, RefCounted) implementing eng §6 end-to-end: export-safe DirAccess scan over res:// (accepts .tres/.res and the export-time .remap pointers so PCK builds work, per §6.1 Web/WASM note), unique non-empty id enforcement via push_error+skip naming the offender(s) (§3.8), tag bucketing in by_tag with idempotent build() and a rebuild() Studio hot-reload hook. select(requested_tags, bb, seen_ids, config) implements §6.2 exactly — tag-union scope (deduped so a multi-tagged event doesn't double its odds), repeatable/seen + pool_conditions filter via JourneyEvaluator.eval_group, empty candidates returns null for the caller's journey_error. Static weighted_pick(candidates, rng) is the §6.3 deterministic cumulative-weight roll using rng.randi_range EXCLUSIVELY (no globals, no fresh RNG — preserves the §1.3 contract that Step 6's save/load round-trips reproduce the stream); negative weights clamp to 0; all-zero-weight pool falls back to uniform so it still resolves (noted in-code). sequence_manager.gd now owns a lazy-built JourneyPoolIndex; the continue_to_pool branch (precedence still forced > target > pool > end, §5.1) calls _route_to_pool which builds on first use, reads seen_ids straight from blackboard.metadata (single source of truth — Step 6's save persists exactly that), and emits journey_error("empty pool for tags: …") with no crash on §6.4 empties. JourneyRuntime.rebuild_pool() now forwards to the manager's rebuild_pool() per §9. Test pool at tests/pool/ (evt_road_bandit/_merchant/_storm/_repeatable) with the spec's tags/weights; evt_road_storm gated by ALL[HAS_FLAG rainy_season], evt_road_merchant exposes a "Bring on the rain" choice that SET_FLAGs rainy_season to make storm eligible without inspector edits. test_config.tres event_pool_dir → res://tests/pool/, evt_start "Take the gold" rerouted to continue_to_pool ["road"] so the existing Step-4 scene drops into the loop. User verified all 7 manual checks (pool loop advancing with changing narrative; non-repeatable bandit/merchant vs repeatable road event; rainy_season-gated storm appearing only after merchant's "Bring on the rain"; tag filter ["trade"] selecting only the merchant; ["void"] empty-pool firing journey_error without crash; "Make camp" terminal exit emitting journey_ended on evt_road_repeatable). DETERMINISM: same-seed runs reproduced the IDENTICAL pulled-event id sequence across two runs with the same click sequence — §1.3 contract holds, and Step 6 save/load can rely on rng.state to reproduce the pull stream.
2026-05-28 — Step 5 follow-ups (from code review pass). Landed 7 must-fixes on the Step 5 surface: (1) start_new_journey now resets _pool_index = JourneyPoolIndex.new() so a second journey with a different event_pool_dir can't reuse the prior config's index (lazy rebuild on first pull). (2) build() no longer latches _built=true on failure — empty dir_path and DirAccess.open failure leave _built=false so the next pull retries (loud but recoverable instead of silently latched-empty); _scan_dir now returns bool to signal root-open success up to build(). (3) _ingest now push_errors when ResourceLoader.load() returns null (distinct from the silent "not a JourneyEvent" skip) so a corrupted .tres surfaces with a clear pool-index diagnostic. (4) build() sorts all_events and each by_tag bucket by String(event.id) at end of scan so candidate ordering is content-determined, not filesystem-determined — required by §1.3 for cross-platform save/load reproducibility under a fixed seed. (5) build() takes a cache_mode param; rebuild() passes ResourceLoader.CACHE_MODE_REPLACE so the §3.7 Studio hot-reload contract actually re-reads from disk (default REUSE path stays untouched for first-load). (6) weighted_pick now push_warnings when total<=0 before the uniform fallback so authors using weight=0 as a "disable" mechanism see the intent-vs-behavior mismatch. (7) evt_road_repeatable gained a "Make camp; rest here" terminal choice so journey_ended is reachable from the pool loop again; orphaned tests/journey/evt_rich.tres deleted. Deferred to follow-up tracking (not blocking): #8 select() _config param unused (signature lie); #9 seen_ids: Array → Array[String], by_tag buckets → Array[JourneyEvent] (type-safety hardening); #10 scope = all_events → .duplicate() (preemptive alias defense); #11 rebuild_pool() docstring claim that it's safe pre-start_new_journey is wrong — either tighten doc or add dir_path overload; #12 empty-pool journey_error message conflates root causes (DX); #13 lazy → eager build tradeoff (UX); #14 String(e.id) duplicated 4× across pool_index/sequence_manager (centralize when Step 6 cements canonical form).
2026-05-28 — Step 6 complete. Added journey_core/save_manager.gd (class_name JourneySaveManager, RefCounted) implementing eng §7 end-to-end: serialize() reduces Blackboard to the §7.1 shape EXACTLY (primitives only — resources/flags copied not aliased; rng_seed pulled out of metadata up to top-level per spec; current_event_id as String per §3.8 so renaming a .tres can't corrupt saves; non-string seen_ids/history entries warn-and-skip), save() opens user://<slot>.dat plaintext when config.save_encryption_key is empty (PRD §5 default — no hardcoded key) else FileAccess.open_encrypted_with_pass, store_var with full_objects defaulting false so a stray non-primitive fails LOUDLY rather than embedding an object reference (§7.2), and returns a meaningful Error on open failure without crashing. load_into() returns ERR_FILE_NOT_FOUND when slot is missing (caller decides), ERR_INVALID_DATA on future-version saves (push_error + refuse), runs _migrate() when save_version < current, then in-place restores resources/flags/metadata + bb.rng.state — restoring rng.state (NOT just the seed) is the §1.3 contract that lets post-load pulls reproduce the saved stream (cashing in Step 5's determinism). _migrate() is the §7.4 ladder scaffold — v1 ships zero steps inside; the while loop with the obvious `if v == N: v = N+1; continue` shape is intentionally empty now so the FIRST breaking change in v2 has a home and shipped player saves never strand; a missing step push_errors and load_into refuses to apply the half-migrated dict. Added JourneyPoolIndex.find_by_id(id_str) — linear scan over all_events; null on miss or pre-build. JourneySequenceManager._enter_event grew an `is_restore: bool = false` second param: when true, skip the turn_counter/history/seen_ids bookkeeping but STILL emit event_changed so the UI rebuilds — normal (non-restore) callers see identical behavior. Added _seq.restore_after_load() (resolves bb.metadata.current_event_id → JourneyEvent via pool first, then BFS over start_event's choice.target_event chain, then resource_defs bottom/top-out events; emits journey_error("save references unknown event id: ...") + ERR_INVALID_DATA on miss) and a get_config() accessor so the runtime can reach save_encryption_key/save_version without poking _config. JourneyRuntime.save_game/load_game now delegate to JourneySaveManager + restore_after_load; both guard against pre-start-of-journey calls with ERR_UNCONFIGURED. tests/game_view.gd extended with S/L keybinds (save/load slot "test") and a serialize()-dict print on save so primitives-only shape can be confirmed without parsing the binary .dat. Also added tests/test_save_load.{gd,tscn} as a headless programmatic suite covering all 6 manual-test points (round-trip / RNG continuity / primitives-only / encryption toggle / missing file / future version) — all 32 checks PASS, including post-load 3-pull sequence matching the control stream byte-for-byte. User then ran the manual game_view scenarios and confirmed: (a) round-trip restores state exactly, (b) post-load pulls reproduce sequence B identically (rng.state continuity holds), (c) serialized dict is primitives + string event id, (d) encryption toggle works both directions, (e) missing/future saves return ERR_FILE_NOT_FOUND/ERR_INVALID_DATA without crash, (f) turn_counter unchanged across restore — the is_restore=false default preserves Step-4/5 behavior while is_restore=true skips bookkeeping correctly. Step 6 fully verified.
2026-05-28 — Step 6 follow-ups (from code review pass — 15-finding ultra review). Landed 11 fixes across Tier-1 correctness, Tier-2 cheap robustness, and Tier-3 docstring tightening: (1, 4, 13) JourneySequenceManager._resolve_event_id rewritten — BFS now seeds from start_event ∪ every pool event ∪ every bottom_out/top_out boundary event so non-pool events reachable only via pool→target chains or boundary chains resolve correctly on load (was: BFS only walked from start_event, so a legitimate save pointing at a deterministic event hanging off a pool event was rejected as "unknown event id"); BFS uses pop_back() not pop_front() (O(1) per pop vs O(n)); _pool_index.build is now guarded with `event_pool_dir != ""` so deterministic-only games don't push_error every load. (3) JourneyRuntime.load_game now snapshots bb.resources/flags/metadata/rng.state/rng.seed BEFORE load_into and rolls back on restore_after_load failure — previously a load_into-success + restore_after_load-failure left the runtime split-brained (UI showed pre-load event, bb held post-load state) and the next process_choice mutated the loaded bb against pre-load semantics. (5) load_game also seeds any config-declared resource missing from the save with its clamped default after load_into succeeds (forward-compat for content updates that add resources before a save migration ships). (2) JourneySaveManager.save now reads f.get_error() after store_var before close and surfaces non-OK — silently-returning-OK on a failed write would lose player state. (6) load_into restores bb.rng.seed alongside bb.rng.state so bb.rng.seed and bb.metadata['rng_seed'] stay in sync (was a two-sources-of-truth split). (8) load_into trims bb.metadata['history'] to HISTORY_CAP on restore so tampered/future-migrated saves can't violate the §10.2 invariant until the next _enter_event trims it. (10) _migrate documented as in-place-mutating, and load_into now passes dict.duplicate(true) so future partial migrations can't escape the function on the error path. (11) load_into refuses a save dict without an 'rng_state' field with ERR_INVALID_DATA (was: silently set state to 0, breaking determinism without surfacing). (12) tests/test_save_load.gd _test_rng_continuity now asserts load_into err==OK before computing replay so a load failure surfaces as a load failure rather than a misleading "RNG continuity broken". Tier-3 docstring tightening: (7) Blackboard.metadata docstring rewritten to declare engine-owned keys (current_event_id, turn_counter, seen_ids, history, rng_seed) the only fields preserved across save/load — codifying the §7.1 contract so custom-metadata callers know up-front that those keys are session-scoped. Two new programmatic checks (rng.seed restored; missing rng_state rejected) — all 38 test_save_load.gd assertions PASS. Deferred to follow-up tracking (not blocking): #9 atomic save write via .tmp + rename_absolute (real hardening, revisit at Step 8 Web/WASM export where tab-close-mid-write becomes likely); #14 JourneyPoolIndex.find_by_id O(n) linear scan → O(1) via parallel by_id Dictionary populated in _ingest (combine with Step-5 follow-up #14 string-coercion centralization); #15 JourneySequenceManager._enter_event is_restore=true bool flag → split _advance_turn / _emit_event_changed methods (altitude refactor; revisit if Step 7 validate() needs the emit-only path independently).
